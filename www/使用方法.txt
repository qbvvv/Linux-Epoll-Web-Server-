终端输入
gcc -o w3 http.c -lpthread -lssl -lcrypto -ljson-c
	如果显示没有安装的话去搜索一下安装指令
./w3
	再打开火狐浏览器输入
	http://localhost:8080/
	进入博客，在班级信息管理中输入文件名等信息。会自动创建一个.txt文件并读入
	输入	http://localhost:8080/secured
	后输入用户名和密码，初始用户名：jyt
			     		初始密码：111
	如果输错了则重新输入。
	如果用户名和密码输入全为空，则显示401
	若输入	http://localhost:8080/adsadas	等错误
	则返回404 not found




